#!/bin/bash

#change the next line to where you want the files to be stored on the local machine
#make sure the write permissions are set
# specify the full absolute path; not a relative path
LOCAL_DEV_STORAGE_DIR="$PWD/temp"
LOCAL_DEV_LOG_DIR="$PWD/temp"
LOCAL_HOST_PORT=8100